default:
    extensions:
        Laracasts\Behat:
            # env_path: .env.behat
        Behat\MinkExtension:
#             default_session: selenium2
             default_session: laravel
             laravel: ~
             base_url: 'http://soaguild.dev/'
             browser_name: 'firefox'
             selenium2:
               capabilities: { "browser": "firefox", "version": "ANY" }

#               selenium.getEval("window.resizeTo(X, Y); window.moveTo(0,0);")